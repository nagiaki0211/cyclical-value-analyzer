"""
アプリ全体の既定設定値。

[要確認] とコメントされている項目は、書籍『50万円を50億円に増やした
投資家の父から娘への教え』の公開情報だけでは具体的な数値基準を
特定できなかったため、著者の基準を厳密に再現したものではない。
あくまで初期値であり、アプリのサイドバーからユーザーが自由に
調整できるようにしている。
"""

DEFAULT_SETTINGS = {
    # PSR(株価売上高倍率 = 時価総額 ÷ 売上高)がこの値を下回ると
    # 「歴史的な割安ゾーン」として強調表示する。
    # [要確認] 書籍中の具体的な倍率基準は公開情報からは特定できないため仮置き。
    "psr_cheap_threshold": 0.5,

    # 景気サイクルの想定周期(年)。書籍の前提である「およそ4年周期」を既定値とする。
    "cycle_length_years": 4,

    # ピーク・ボトム検出時に「山」「谷」とみなす最小の変動幅(業績指標に対する比率)。
    # 小さすぎるとノイズを山谷として誤検出するため、ある程度の閾値を設ける。
    "peak_trough_prominence_ratio": 0.10,

    # 減益/赤字とみなす連続期間数(スクリーニングで「業績不振」と判定する条件)。
    "consecutive_decline_periods": 2,

    # 業種平均PSRの比較に使う同業種ティッカーのサンプル数上限。
    "industry_peer_sample_size": 5,
}
